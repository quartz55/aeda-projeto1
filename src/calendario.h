#ifndef _CALENDARIO_H_
#define _CALENDARIO_H_

#include <vector>
#include <string>

using std::vector; using std::string;

class Calendario{
protected:

    static const string nomes[13];
    static const int daysOfMonth[13];
    static const int daysOfMonthL[13];

public:

    //Calendario();

};

#endif
